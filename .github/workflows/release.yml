name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    name: Build (${{ matrix.target }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact_name: nu-mcp
            asset_name: nu-mcp-linux-x64
          - os: macos-latest
            target: x86_64-apple-darwin
            artifact_name: nu-mcp
            asset_name: nu-mcp-macos-x64
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact_name: nu-mcp.exe
            asset_name: nu-mcp-windows-x64

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Build
        run: |
          cargo build --release --target ${{ matrix.target }}

      - name: Prepare artifact
        shell: bash
        run: |
          cd target/${{ matrix.target }}/release
          if [ "${{ runner.os }}" = "Windows" ]; then
            7z a ../../${{ matrix.asset_name }}.zip ${{ matrix.artifact_name }}
          else
            tar czf ../../${{ matrix.asset_name }}.tar.gz ${{ matrix.artifact_name }}
          fi
          cd ../../..

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset_name }}
          path: ${{ matrix.asset_name }}.*

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          find artifacts -type f -name "*.tar.gz" -o -name "*.zip" > files.txt
          cat files.txt
          if [ -s files.txt ]; then
            gh release create '${{ github.ref_name }}' \
              --repo '${{ github.repository }}' \
              --notes "Release ${{ github.ref_name }}" \
              $(cat files.txt | head -10 | xargs -I {} echo -n "--assets {} ")
          else
            echo "No artifacts found, creating release without assets"
            gh release create '${{ github.ref_name }}' \
              --repo '${{ github.repository }}' \
              --notes "Release ${{ github.ref_name }}"
          fi

  publish-npm:
    name: Publish to npm
    needs: build
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'

      - name: Copy npm package files
        run: |
          cp -r npm/* .
          cp README.md npm/README.md

      - name: Publish to npm
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm publish
